[build]
builder = "nixpacks"

[deploy]
startCommand = "cd src && uvicorn api.app:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"